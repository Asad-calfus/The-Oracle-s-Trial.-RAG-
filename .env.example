OPENAI_API_KEY=sk-your-key-here

# PostgreSQL connection for chat thread persistence.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# (password can be left out when connecting locally as your own OS user)
DATABASE_URL=postgresql://localhost:5432/smartdoc
